A Lisp to JavaScript 1.5 compiler.

Requires SpiderMonkey.

Currently, the coolest program it can run is

(defclass <dog>)
(def say-something ((<dog> d)) "wooof")

(defclass <cat>)
(def say-something ((<cat> c)) "meoow")

(say-something (new <dog>))
(say-something (new <cat>))

-- Manuel Simoni, 2008-10-15
